{
  "main": "index.js",
  "scripts": {
    "start": "micro",
    "build": "docker build . -t ${CAADE_REGISTRY}/c3-environment-manager",
    "deploy": "docker push ${CAADE_REGISTRY}/c3-environment-manager",
    "publish": "docker push ${CAADE_REGISTRY}/c3-environment-manager"
  },
  "dependencies": {
    "micro": "latest",
    "node-fetch": "latest"
  }
}
