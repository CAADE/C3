{
  "main": "index.js",
  "scripts": {
    "start": "micro",
    "build": "docker build . -t ${CAADE_REGISTRY}/c3-application-manager",
    "deploy": "docker push ${CAADE_REGISTRY}/c3-application-manager",
    "publish": "docker push ${CAADE_REGISTRY}/c3-application-manager"
  },
  "dependencies": {
    "micro": "latest",
    "node-fetch": "latest"
  }
}
