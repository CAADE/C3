{
  "main": "index.js",
  "scripts": {
    "start": "micro",
    "build": "docker build . -t ${CAADE_REGISTRY}/c3/identity-manager",
    "deploy": "docker push ${CAADE_REGISTRY}/c3/identity-manager",
    "publish": "docker push ${CAADE_REGISTRY}/c3/identity-manager"
  },
  "dependencies": {
    "micro": "latest",
    "node-fetch": "latest"
  }
}
