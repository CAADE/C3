var Promise = require('bluebird');

describe('Use Case Manage-Clouds', function () {
  describe('List-Clouds Scenario', function () {
    it('List-Clouds Primary Test', function (done) {
       done();
    });
  });
});
