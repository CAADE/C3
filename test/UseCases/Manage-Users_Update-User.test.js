var Promise = require('bluebird');

describe('Use Case Manage-Users', function () {
  describe('Update-User Scenario', function () {
    it('Update-User Primary Test', function (done) {
       done();
    });
  });
});
