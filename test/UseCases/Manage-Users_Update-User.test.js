var Promise = require('bluebird');

describe('Use Case Manage-Users', () => {
  describe('Update-User Scenario', () => {
    it('Update-User Primary Test', (done) => {
      done();
    });
  });
});
