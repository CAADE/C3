var Promise = require('bluebird');

describe('Use Case Manage-Environments', function () {
  describe('Show-Environment-Processes Scenario', function () {
    it('Show-Environment-Processes Primary Test', function (done) {
       done();
    });
  });
});
