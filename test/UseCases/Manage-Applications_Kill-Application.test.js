var Promise = require('bluebird');

describe('Use Case Manage-Applications', function () {
  describe('Kill-Application Scenario', function () {
    it('Kill-Application Primary Test', function (done) {
       done();
    });
  });
});
