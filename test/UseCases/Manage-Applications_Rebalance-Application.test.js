var Promise = require('bluebird');

describe('Use Case Manage-Applications', () => {
  describe('Rebalance-Application Scenario', () => {
    it('Rebalance-Application Primary Test', (done) => {
      done();
    });
  });
});
