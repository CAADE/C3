var Promise = require('bluebird');

describe('Use Case Manage-Environments', function () {
  describe('Show-Environment Scenario', function () {
    it('Show-Environment Primary Test', function (done) {
       done();
    });
  });
});
